import React, {useState} from 'react';
import { View, Text } from 'react-native';

const SunScreen = ({navigation, route}) => {

  return (
    <View>
      <Text>Heres our first planet screen</Text>
    </View>
  );
};

export default SunScreen;
